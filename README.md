Very basic version of the app, just to get it saved.
Currently, it can:

-   search coins by name using coingecko API
-   add coins to user's list (localStorage for now)
-   remove coins from user's list
-   display user's list of coins
